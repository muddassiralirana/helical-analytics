import React from "react";

const GenerateKeySteps = () => {
  return (
    <>
      <h6 className="text-info">Tip: (Share & Receive Data)</h6>
      <ol>
        <li>
          Request connection from Submission Site (NEW connection from
          Submission site)
        </li>
        <li>
          Upload "connection config file" from submission site into (receive
          connection)
        </li>
        <li>Generate "connection config file" for the submission Site</li>
      </ol>
    </>
  );
};

export default GenerateKeySteps;
